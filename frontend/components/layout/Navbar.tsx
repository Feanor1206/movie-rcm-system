'use client';
import Link from 'next/link'; import { Menu, Search, UserRound, X } from 'lucide-react'; import { useState } from 'react';
const links = [['Home','/'],['Movies','/search'],['Genres','/search']];
export function Navbar() { const [open,setOpen]=useState(false); return <header className="sticky top-0 z-50 border-b border-zinc-800 bg-zinc-950/90 backdrop-blur-xl"><nav className="mx-auto flex h-20 max-w-6xl items-center justify-between px-5 sm:px-8"><Link href="/" className="font-editorial text-2xl font-semibold tracking-[-.04em]">Dippie<span className="text-rose-600">.</span> <span className="ml-2 font-mono text-[8px] uppercase tracking-[.24em] text-zinc-500">Cinema / 2026</span></Link><div className="hidden items-center gap-7 md:flex">{links.map(([name,href],i)=><Link key={name} href={href} className="font-mono text-[11px] text-zinc-400 transition hover:text-white first:text-white"><span className="mr-1 text-rose-500">0{i+1}</span> {name}</Link>)}</div><div className="hidden items-center gap-3 md:flex"><Link aria-label="Search movies" href="/search" className="p-2 text-zinc-300 transition hover:text-rose-500"><Search size={19}/></Link><Link href="/profile" aria-label="Profile" className="border border-zinc-600 px-4 py-2 font-mono text-[10px] uppercase tracking-wider text-zinc-200 transition hover:border-rose-600 hover:text-white"><UserRound className="mr-2 inline" size={13}/> Profile</Link></div><button aria-label="Open navigation" onClick={()=>setOpen(!open)} className="rounded-lg p-2 md:hidden">{open?<X/>:<Menu/>}</button></nav>{open&&<div className="border-t border-zinc-800 bg-zinc-950 px-5 py-4 md:hidden"><div className="flex flex-col gap-3">{links.map(([name,href])=><Link onClick={()=>setOpen(false)} key={name} href={href} className="py-2 text-zinc-300">{name}</Link>)}<Link href="/profile" className="py-2 text-zinc-300">Profile</Link></div></div>}</header>; }
